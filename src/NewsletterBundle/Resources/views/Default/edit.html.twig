{# src/NewsletterBundle/Resources/views/Default/edit.html.twig #}

{% extends "NewsletterBundle::layout.html.twig" %}

{% block title %}
  Modifier une annonce - {{ parent() }}
{% endblock %}

{% block newsletter_body %}

  <h2>Modifier une newsletter</h2>

  {{ include("NewsletterBundle:Default:form.html.twig") }}

  <p>
    <a href="{{ path('newsletter_view', {'id': campaign.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la newsletter
    </a>
  </p>

{% endblock %}